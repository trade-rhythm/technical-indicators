import type { JSONDef, Indicator } from "../types"; export interface
{{name}}Args {} export default class
{{name}}
implements Indicator { constructor() {} next(value: number): number {} toJSON():
JSONDef { return { $type: "finance.tr.{{name}}", }; } static from({}:
{{name}}Args):
{{name}}
{ return new
{{name}}(); } }